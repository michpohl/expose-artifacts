name: 'List artifacts of a workflow'
description: 'A github action to list artifacts gerenated on a pull request'
author: 'Michael Pohl'
branding:
  icon: 'edit'
  color: 'red'
runs:
  using: 'node12'
  main: 'dist/index.js'
inputs:
  github-token:
    description: 'Your GITHUB_TOKEN secret'
    required: true